name: CoderCo Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4


    - name:  hello world custom action
      uses: mahameddalmar20034-cpu/coderco-customv2@main
      with:  
        
        who-to-greet: 'Coderco Team'



    